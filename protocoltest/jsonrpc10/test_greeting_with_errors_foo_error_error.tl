-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: awsJson1_0

local types = require("jsonrpc10.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.awsjson")
local protocol = protocol_mod.new({ version = "1.0" })

local service = schema.service({
    id = shape_id.from("aws.protocoltests.json10", "JsonRpc10"),
    version = "2020-07-14",
    traits = {},
})

describe("GreetingWithErrors FooError error", function()
    it("AwsJson10FooErrorUsingXAmznErrorType", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "FooError",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorUsingXAmznErrorTypeWithUri", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorUsingXAmznErrorTypeWithUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorUsingCode", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "code": "FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorUsingCodeAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "code": "aws.protocoltests.json10#FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorUsingCodeUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "code": "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorWithDunderType", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "__type": "FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorWithDunderTypeAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json10#FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorWithDunderTypeUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json10#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson10FooErrorWithNestedTypeProperty", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.0",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json10#FooError",
                "ErrorDetails": [
                  {
                      "__type": "com.amazon.internal#ErrorDetails",
                      "reason": "Some reason"
                  }
                ]
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json10", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

end)
