-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restJson1

local types = require("restjsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restjson")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restjson", "RestJson"),
    version = "2019-12-16",
    traits = {},
})

describe("JsonUnions response", function()
    it("RestJsonDeserializeStringUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "stringValue": "foo"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            stringValue = "foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeBooleanUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "booleanValue": true
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            booleanValue = true,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeNumberUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "numberValue": 1
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            numberValue = 1,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeBlobUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "blobValue": "Zm9v"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            blobValue = "foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeTimestampUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "timestampValue": 1398796238
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            timestampValue = 1398796238,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeEnumUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "enumValue": "Foo"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            enumValue = "Foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeListUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "listValue": ["foo", "bar"]
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            listValue = {
            "foo",
            "bar",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeMapUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "mapValue": {
                        "foo": "bar",
                        "spam": "eggs"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            mapValue = {
            foo = "bar",
            spam = "eggs",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeStructureUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "structureValue": {
                        "hi": "hello"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            structureValue = {
            hi = "hello",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonDeserializeIgnoreType", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "contents": {
                    "__type": "aws.protocoltests.json10#MyUnion",
                    "structureValue": {
                        "hi": "hello"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            structureValue = {
            hi = "hello",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

end)
