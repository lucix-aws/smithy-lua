-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Generated protocol test file — do not edit
-- Protocol: restJson1

local types = require("restjsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restjson")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restjson", "RestJson"),
    version = "2019-12-16",
    traits = {},
})

describe("SimpleScalarProperties request", function()
    it("RestJsonSimpleScalarProperties", function()
        local input = {
            foo = "Foo",
            stringValue = "string",
            trueBooleanValue = true,
            falseBooleanValue = false,
            byteValue = 1,
            shortValue = 2,
            integerValue = 3,
            longValue = 4,
            floatValue = 5.5,
            doubleValue = 6.5,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "SimpleScalarProperties"),
            input = types.SimpleScalarPropertiesInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "PUT", path = "/SimpleScalarProperties" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("PUT", request.method)
        h.assert_url_path(request.url, "/SimpleScalarProperties")
        h.assert_header(request, "Content-Type", "application/json")
        h.assert_header(request, "X-Foo", "Foo")
        local body_str = h.read_body(request.body)
        h.assert_json_eq(body_str, [[{
            "stringValue": "string",
            "trueBooleanValue": true,
            "falseBooleanValue": false,
            "byteValue": 1,
            "shortValue": 2,
            "integerValue": 3,
            "longValue": 4,
            "floatValue": 5.5,
            "DoubleDribble": 6.5
        }]])
    end)

    it("RestJsonDoesntSerializeNullStructureValues", function()
        local input = {
            stringValue = nil,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "SimpleScalarProperties"),
            input = types.SimpleScalarPropertiesInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "PUT", path = "/SimpleScalarProperties" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("PUT", request.method)
        h.assert_url_path(request.url, "/SimpleScalarProperties")
        h.assert_header(request, "Content-Type", "application/json")
        local body_str = h.read_body(request.body)
        h.assert_json_eq(body_str, [[{}]])
    end)

    it("RestJsonSupportsNaNFloatInputs", function()
        local input = {
            floatValue = 0/0,
            doubleValue = 0/0,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "SimpleScalarProperties"),
            input = types.SimpleScalarPropertiesInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "PUT", path = "/SimpleScalarProperties" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("PUT", request.method)
        h.assert_url_path(request.url, "/SimpleScalarProperties")
        h.assert_header(request, "Content-Type", "application/json")
        local body_str = h.read_body(request.body)
        h.assert_json_eq(body_str, [[{
            "floatValue": "NaN",
            "DoubleDribble": "NaN"
        }]])
    end)

    it("RestJsonSupportsInfinityFloatInputs", function()
        local input = {
            floatValue = math.huge,
            doubleValue = math.huge,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "SimpleScalarProperties"),
            input = types.SimpleScalarPropertiesInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "PUT", path = "/SimpleScalarProperties" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("PUT", request.method)
        h.assert_url_path(request.url, "/SimpleScalarProperties")
        h.assert_header(request, "Content-Type", "application/json")
        local body_str = h.read_body(request.body)
        h.assert_json_eq(body_str, [[{
            "floatValue": "Infinity",
            "DoubleDribble": "Infinity"
        }]])
    end)

    it("RestJsonSupportsNegativeInfinityFloatInputs", function()
        local input = {
            floatValue = -math.huge,
            doubleValue = -math.huge,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "SimpleScalarProperties"),
            input = types.SimpleScalarPropertiesInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "PUT", path = "/SimpleScalarProperties" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("PUT", request.method)
        h.assert_url_path(request.url, "/SimpleScalarProperties")
        h.assert_header(request, "Content-Type", "application/json")
        local body_str = h.read_body(request.body)
        h.assert_json_eq(body_str, [[{
            "floatValue": "-Infinity",
            "DoubleDribble": "-Infinity"
        }]])
    end)

end)
