-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restJson1

local types = require("restjsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restjson")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restjson", "RestJson"),
    version = "2019-12-16",
    traits = {},
})

describe("InputAndOutputWithHeaders request", function()
    it("RestJsonInputAndOutputWithStringHeaders", function()
        local input = {
            headerString = "Hello",
            headerStringList = {
            "a",
            "b",
            "c",
        },
            headerStringSet = {
            "a",
            "b",
            "c",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-String", "Hello")
        h.assert_header(request, "X-StringList", "a, b, c")
        h.assert_header(request, "X-StringSet", "a, b, c")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithQuotedStringHeaders", function()
        local input = {
            headerStringList = {
            "b,c",
            "\"def\"",
            "a",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-StringList", "\"b,c\", \"\\\"def\\\"\", a")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithNumericHeaders", function()
        local input = {
            headerByte = 1,
            headerShort = 123,
            headerInteger = 123,
            headerLong = 123,
            headerFloat = 1.1,
            headerDouble = 1.1,
            headerIntegerList = {
            1,
            2,
            3,
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Byte", "1")
        h.assert_header(request, "X-Double", "1.1")
        h.assert_header(request, "X-Float", "1.1")
        h.assert_header(request, "X-Integer", "123")
        h.assert_header(request, "X-IntegerList", "1, 2, 3")
        h.assert_header(request, "X-Long", "123")
        h.assert_header(request, "X-Short", "123")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithBooleanHeaders", function()
        local input = {
            headerTrueBool = true,
            headerFalseBool = false,
            headerBooleanList = {
            true,
            false,
            true,
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Boolean1", "true")
        h.assert_header(request, "X-Boolean2", "false")
        h.assert_header(request, "X-BooleanList", "true, false, true")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithTimestampHeaders", function()
        local input = {
            headerTimestampList = {
            1576540098,
            1576540098,
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-TimestampList", "Mon, 16 Dec 2019 23:48:18 GMT, Mon, 16 Dec 2019 23:48:18 GMT")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithEnumHeaders", function()
        local input = {
            headerEnum = "Foo",
            headerEnumList = {
            "Foo",
            "Bar",
            "Baz",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Enum", "Foo")
        h.assert_header(request, "X-EnumList", "Foo, Bar, Baz")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonInputAndOutputWithIntEnumHeaders", function()
        local input = {
            headerIntegerEnum = 1,
            headerIntegerEnumList = {
            1,
            2,
            3,
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-IntegerEnum", "1")
        h.assert_header(request, "X-IntegerEnumList", "1, 2, 3")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonSupportsNaNFloatHeaderInputs", function()
        local input = {
            headerFloat = 0/0,
            headerDouble = 0/0,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Double", "NaN")
        h.assert_header(request, "X-Float", "NaN")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonSupportsInfinityFloatHeaderInputs", function()
        local input = {
            headerFloat = math.huge,
            headerDouble = math.huge,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Double", "Infinity")
        h.assert_header(request, "X-Float", "Infinity")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

    it("RestJsonSupportsNegativeInfinityFloatHeaderInputs", function()
        local input = {
            headerFloat = -math.huge,
            headerDouble = -math.huge,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "InputAndOutputWithHeaders"),
            input = types.InputAndOutputWithHeadersInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/InputAndOutputWithHeaders" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/InputAndOutputWithHeaders")
        h.assert_header(request, "X-Double", "-Infinity")
        h.assert_header(request, "X-Float", "-Infinity")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

end)
