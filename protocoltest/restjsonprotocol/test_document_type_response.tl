-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restJson1

local types = require("restjsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restjson")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restjson", "RestJson"),
    version = "2019-12-16",
    traits = {},
})

describe("DocumentType response", function()
    it("DocumentOutput", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "stringValue": "string",
                "documentValue": {
                    "foo": "bar"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "DocumentType"),
            input = schema.new({ type = "structure" }),
            output = types.DocumentTypeOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            stringValue = "string",
            documentValue = {
            foo = "bar",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("DocumentOutputString", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "stringValue": "string",
                "documentValue": "hello"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "DocumentType"),
            input = schema.new({ type = "structure" }),
            output = types.DocumentTypeOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            stringValue = "string",
            documentValue = "hello",
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("DocumentOutputNumber", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "stringValue": "string",
                "documentValue": 10
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "DocumentType"),
            input = schema.new({ type = "structure" }),
            output = types.DocumentTypeOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            stringValue = "string",
            documentValue = 10,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("DocumentOutputBoolean", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "stringValue": "string",
                "documentValue": false
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "DocumentType"),
            input = schema.new({ type = "structure" }),
            output = types.DocumentTypeOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            stringValue = "string",
            documentValue = false,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("DocumentOutputArray", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "stringValue": "string",
                "documentValue": [
                    true,
                    false
                ]
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "DocumentType"),
            input = schema.new({ type = "structure" }),
            output = types.DocumentTypeOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            stringValue = "string",
            documentValue = {
            true,
            false,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

end)
