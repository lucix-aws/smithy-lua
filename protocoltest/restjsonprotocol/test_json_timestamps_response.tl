-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restJson1

local types = require("restjsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restjson")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restjson", "RestJson"),
    version = "2019-12-16",
    traits = {},
})

describe("JsonTimestamps response", function()
    it("RestJsonJsonTimestamps", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "normal": 1398796238
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            normal = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithDateTimeFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "dateTime": "2014-04-29T18:30:38Z"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            dateTime = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithDateTimeOnTargetFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "dateTimeOnTarget": "2014-04-29T18:30:38Z"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            dateTimeOnTarget = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithEpochSecondsFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "epochSeconds": 1398796238
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            epochSeconds = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithEpochSecondsOnTargetFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "epochSecondsOnTarget": 1398796238
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            epochSecondsOnTarget = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithHttpDateFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "httpDate": "Tue, 29 Apr 2014 18:30:38 GMT"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            httpDate = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestJsonJsonTimestampsWithHttpDateOnTargetFormat", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/json",
            },
            body = http.string_reader([[{
                "httpDateOnTarget": "Tue, 29 Apr 2014 18:30:38 GMT"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restjson", "JsonTimestamps"),
            input = schema.new({ type = "structure" }),
            output = types.JsonTimestampsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            httpDateOnTarget = 1398796238,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

end)
