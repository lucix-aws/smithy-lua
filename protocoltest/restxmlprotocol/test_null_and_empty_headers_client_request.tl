-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restXml

local types = require("restxmlprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restxml")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restxml", "RestXml"),
    version = "2019-12-16",
    traits = {},
})

describe("NullAndEmptyHeadersClient request", function()
    it("NullAndEmptyHeaders", function()
        local input = {
            a = nil,
            b = "",
            c = {},
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "NullAndEmptyHeadersClient"),
            input = types.NullAndEmptyHeadersClientInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "GET", path = "/NullAndEmptyHeadersClient" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("GET", request.method)
        h.assert_url_path(request.url, "/NullAndEmptyHeadersClient")
        h.assert_header(request, "X-B", "")
        h.assert_header(request, "X-C", "")
        h.assert_no_header(request, "X-A")
        local body_str = h.read_body(request.body)
        assert.is_true(body_str == "" or body_str == nil, "expected empty body")
    end)

end)
