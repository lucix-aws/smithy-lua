-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restXml

local types = require("restxmlprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restxml")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restxml", "RestXml"),
    version = "2019-12-16",
    traits = {},
})

describe("XmlTimestamps request", function()
    it("XmlTimestamps", function()
        local input = {
            normal = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <normal>2014-04-29T18:30:38Z</normal>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithDateTimeFormat", function()
        local input = {
            dateTime = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <dateTime>2014-04-29T18:30:38Z</dateTime>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithDateTimeOnTargetFormat", function()
        local input = {
            dateTimeOnTarget = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <dateTimeOnTarget>2014-04-29T18:30:38Z</dateTimeOnTarget>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithEpochSecondsFormat", function()
        local input = {
            epochSeconds = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <epochSeconds>1398796238</epochSeconds>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithEpochSecondsOnTargetFormat", function()
        local input = {
            epochSecondsOnTarget = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <epochSecondsOnTarget>1398796238</epochSecondsOnTarget>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithHttpDateFormat", function()
        local input = {
            httpDate = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <httpDate>Tue, 29 Apr 2014 18:30:38 GMT</httpDate>
        </XmlTimestampsRequest>
        ]])
    end)

    it("XmlTimestampsWithHttpDateOnTargetFormat", function()
        local input = {
            httpDateOnTarget = 1398796238,
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "XmlTimestamps"),
            input = types.XmlTimestampsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/XmlTimestamps" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/XmlTimestamps")
        h.assert_header(request, "Content-Type", "application/xml")
        local body_str = h.read_body(request.body)
        h.assert_xml_eq(body_str, [[<XmlTimestampsRequest>
            <httpDateOnTarget>Tue, 29 Apr 2014 18:30:38 GMT</httpDateOnTarget>
        </XmlTimestampsRequest>
        ]])
    end)

end)
