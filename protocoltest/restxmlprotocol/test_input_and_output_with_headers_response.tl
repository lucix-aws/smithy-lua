-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: restXml

local types = require("restxmlprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.restxml")
local protocol = protocol_mod.new()

local service = schema.service({
    id = shape_id.from("aws.protocoltests.restxml", "RestXml"),
    version = "2019-12-16",
    traits = {},
})

describe("InputAndOutputWithHeaders response", function()
    it("InputAndOutputWithStringHeaders", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-String"] = "Hello",
                ["X-StringList"] = "a, b, c",
                ["X-StringSet"] = "a, b, c",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerString = "Hello",
            headerStringList = {
            "a",
            "b",
            "c",
        },
            headerStringSet = {
            "a",
            "b",
            "c",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("InputAndOutputWithNumericHeaders", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Byte"] = "1",
                ["X-Double"] = "1.1",
                ["X-Float"] = "1.1",
                ["X-Integer"] = "123",
                ["X-IntegerList"] = "1, 2, 3",
                ["X-Long"] = "123",
                ["X-Short"] = "123",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerByte = 1,
            headerShort = 123,
            headerInteger = 123,
            headerLong = 123,
            headerFloat = 1.1,
            headerDouble = 1.1,
            headerIntegerList = {
            1,
            2,
            3,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("InputAndOutputWithBooleanHeaders", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Boolean1"] = "true",
                ["X-Boolean2"] = "false",
                ["X-BooleanList"] = "true, false, true",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerTrueBool = true,
            headerFalseBool = false,
            headerBooleanList = {
            true,
            false,
            true,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("InputAndOutputWithTimestampHeaders", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-TimestampList"] = "Mon, 16 Dec 2019 23:48:18 GMT, Mon, 16 Dec 2019 23:48:18 GMT",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerTimestampList = {
            1576540098,
            1576540098,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("InputAndOutputWithEnumHeaders", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Enum"] = "Foo",
                ["X-EnumList"] = "Foo, Bar, Baz",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerEnum = "Foo",
            headerEnumList = {
            "Foo",
            "Bar",
            "Baz",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestXmlSupportsNaNFloatHeaderOutputs", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Double"] = "NaN",
                ["X-Float"] = "NaN",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerFloat = 0/0,
            headerDouble = 0/0,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestXmlSupportsInfinityFloatHeaderOutputs", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Double"] = "Infinity",
                ["X-Float"] = "Infinity",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerFloat = math.huge,
            headerDouble = math.huge,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("RestXmlSupportsNegativeInfinityFloatHeaderOutputs", function()
        local response = {
            status_code = 200,
            headers = {
                ["X-Double"] = "-Infinity",
                ["X-Float"] = "-Infinity",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.restxml", "InputAndOutputWithHeaders"),
            input = schema.new({ type = "structure" }),
            output = types.InputAndOutputWithHeadersOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            headerFloat = -math.huge,
            headerDouble = -math.huge,
        }
        h.assert_deep_eq(output, expected, "output")
    end)

end)
