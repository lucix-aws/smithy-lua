-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Generated protocol test file — do not edit
-- Protocol: awsQuery

local types = require("queryprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.awsquery")
local protocol = protocol_mod.new({ version = "2020-01-08" })

local service = schema.service({
    id = shape_id.from("aws.protocoltests.query", "AwsQuery"),
    version = "2020-01-08",
    traits = {},
})

describe("QueryLists request", function()
    it("QueryLists", function()
        local input = {
            ListArg = {
            "foo",
            "bar",
            "baz",
        },
            ComplexListArg = {
            {
            hi = "hello",
        },
            {
            hi = "hola",
        },
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&ListArg.member.1=foo&ListArg.member.2=bar&ListArg.member.3=baz&ComplexListArg.member.1.hi=hello&ComplexListArg.member.2.hi=hola]])
    end)

    it("EmptyQueryLists", function()
        local input = {
            ListArg = {},
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&ListArg=]])
    end)

    it("FlattenedQueryLists", function()
        local input = {
            FlattenedListArg = {
            "A",
            "B",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&FlattenedListArg.1=A&FlattenedListArg.2=B]])
    end)

    it("QueryListArgWithXmlNameMember", function()
        local input = {
            ListArgWithXmlNameMember = {
            "A",
            "B",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&ListArgWithXmlNameMember.item.1=A&ListArgWithXmlNameMember.item.2=B]])
    end)

    it("QueryFlattenedListArgWithXmlName", function()
        local input = {
            FlattenedListArgWithXmlName = {
            "A",
            "B",
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&Hi.1=A&Hi.2=B]])
    end)

    it("QueryNestedStructWithList", function()
        local input = {
            NestedWithList = {
            ListArg = {
            "A",
            "B",
        },
        },
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.query", "QueryLists"),
            input = types.QueryListsInput,
            output = schema.new({ type = "structure" }),
            traits = {
                [traits.HTTP] = { method = "POST", path = "/" },
            },
        })
        local request, err = protocol:serialize(input, service, operation)
        assert.is_nil(err, "serialize error: " .. tostring(err))
        assert.are.equal("POST", request.method)
        h.assert_url_path(request.url, "/")
        h.assert_header(request, "Content-Type", "application/x-www-form-urlencoded")
        local body_str = h.read_body(request.body)
        h.assert_form_eq(body_str, [[Action=QueryLists&Version=2020-01-08&NestedWithList.ListArg.member.1=A&NestedWithList.ListArg.member.2=B]])
    end)

end)
