-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Generated protocol test file — do not edit
-- Protocol: awsJson1_1

local types = require("jsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.awsjson")
local protocol = protocol_mod.new({ version = "1.1" })

local service = schema.service({
    id = shape_id.from("aws.protocoltests.json", "JsonProtocol"),
    version = "2018-01-01",
    traits = {},
})

describe("GreetingWithErrors FooError error", function()
    it("AwsJson11FooErrorUsingXAmznErrorType", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "FooError",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorUsingXAmznErrorTypeWithUri", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorUsingXAmznErrorTypeWithUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["X-Amzn-Errortype"] = "aws.protocoltests.json#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/",
            },
            body = http.string_reader([[]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorUsingCode", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "code": "FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorUsingCodeAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "code": "aws.protocoltests.json#FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorUsingCodeUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "code": "aws.protocoltests.json#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorWithDunderType", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "__type": "FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorWithDunderTypeAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json#FooError"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorWithDunderTypeUriAndNamespace", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json#FooError:http://internal.amazon.com/coral/com.amazon.coral.validate/"
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

    it("AwsJson11FooErrorWithNestedTypeProperty", function()
        local response = {
            status_code = 500,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "__type": "aws.protocoltests.json#FooError",
                "ErrorDetails": [
                  {
                      "__type": "com.amazon.internal#ErrorDetails",
                      "reason": "Some reason"
                  }
                ]
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "GreetingWithErrors"),
            input = schema.new({ type = "structure" }),
            output = types.FooError,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(output, "expected nil output for error response")
        assert.is_not_nil(err, "expected error")
        assert.are.equal("api", err.type)
        assert.is_true(err.code == "FooError" or (err.code and err.code:find("FooError", 1, true) ~= nil),
            "error code: expected FooError, got: " .. tostring(err.code))
    end)

end)
