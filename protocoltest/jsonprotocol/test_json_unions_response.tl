-- Code generated by lua-client-codegen. DO NOT EDIT.

local assert = require("luassert") as any

-- Generated protocol test file — do not edit
-- Protocol: awsJson1_1

local types = require("jsonprotocol.schemas")
local schema = require("smithy.schema")
local shape_id = require("smithy.shape_id")
local traits = require("smithy.traits")
local http = require("smithy.http")
local h = require("smithy.testing")
local protocol_mod = require("smithy.protocol.awsjson")
local protocol = protocol_mod.new({ version = "1.1" })

local service = schema.service({
    id = shape_id.from("aws.protocoltests.json", "JsonProtocol"),
    version = "2018-01-01",
    traits = {},
})

describe("JsonUnions response", function()
    it("AwsJson11DeserializeStringUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "stringValue": "foo"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            stringValue = "foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeBooleanUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "booleanValue": true
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            booleanValue = true,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeNumberUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "numberValue": 1
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            numberValue = 1,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeBlobUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "blobValue": "Zm9v"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            blobValue = "foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeTimestampUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "timestampValue": 1398796238
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            timestampValue = 1398796238,
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeEnumUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "enumValue": "Foo"
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            enumValue = "Foo",
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeListUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "listValue": ["foo", "bar"]
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            listValue = {
            "foo",
            "bar",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeMapUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "mapValue": {
                        "foo": "bar",
                        "spam": "eggs"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            mapValue = {
            foo = "bar",
            spam = "eggs",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeStructureUnionValue", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "structureValue": {
                        "hi": "hello"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            structureValue = {
            hi = "hello",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

    it("AwsJson11DeserializeIgnoreType", function()
        local response = {
            status_code = 200,
            headers = {
                ["Content-Type"] = "application/x-amz-json-1.1",
            },
            body = http.string_reader([[{
                "contents": {
                    "__type": "aws.protocoltests.json10#MyUnion",
                    "structureValue": {
                        "hi": "hello"
                    }
                }
            }]]),
        }
        local operation = schema.operation({
            id = shape_id.from("aws.protocoltests.json", "JsonUnions"),
            input = schema.new({ type = "structure" }),
            output = types.JsonUnionsOutput,
            traits = {},
        })
        local output, err = protocol:deserialize(response, operation)
        assert.is_nil(err, "deserialize error: " .. tostring(err and err.message or err))
        local expected = {
            contents = {
            structureValue = {
            hi = "hello",
        },
        },
        }
        h.assert_deep_eq(output, expected, "output")
    end)

end)
