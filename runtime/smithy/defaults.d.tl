-- Teal declarations for smithy-lua runtime/defaults

local record M

    resolve_auth_schemes: function(cfg: table)
    resolve_identity_resolvers: function(cfg: table)
    resolve_http_client: function(cfg: table)
    resolve_retry_strategy: function(cfg: table)

end

return M
